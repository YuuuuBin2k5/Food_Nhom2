CREATE TABLE IF NOT EXISTS cart_items (cart_item_id BIGINT AUTO_INCREMENT PRIMARY KEY, user_id VARCHAR(255) NOT NULL, product_id BIGINT NOT NULL, quantity INT NOT NULL DEFAULT 1, FOREIGN KEY (product_id) REFERENCES products(product_id) ON DELETE CASCADE);
